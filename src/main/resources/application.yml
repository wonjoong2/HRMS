openai:
  api-key: ${OPENAI_KEY}

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: wjkim0331@gmail.com
    password: ${GMAIL_PASS}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  endpoints:
    web:
      exposure:
        include: health, beans, httptrace, info, metrics, prometheus


